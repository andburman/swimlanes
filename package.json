{
  "name": "@graph-tl/graph",
  "version": "0.2.6",
  "description": "Agent-native persistent task graph. MCP server for agent planning and execution across sessions.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "graph": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "homepage": "https://github.com/Graph-tl/graph",
  "repository": {
    "type": "git",
    "url": "https://github.com/Graph-tl/graph.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup && cd ui && npx tsc -b && npx vite build",
    "build:server": "tsup",
    "build:ui": "cd ui && npx tsc -b && npx vite build",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "agents",
    "task-graph",
    "planning",
    "ai-agents",
    "claude"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "better-sqlite3": "^12.6.2",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.2.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
